[project]
name = "package-a"
version = "0.1.0"
description = "Slang test fixture package A"
requires-python = ">=3.12"
dependencies = [
    "spm",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
