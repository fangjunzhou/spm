[project]
name = "package-e"
version = "0.1.0"
description = "Slang test fixture package E"
requires-python = ">=3.12"
dependencies = ["spm-slang"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
