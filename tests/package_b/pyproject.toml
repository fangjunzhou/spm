[project]
name = "package-b"
version = "0.1.0"
description = "Slang test fixture package B"
requires-python = ">=3.12"
dependencies = [
    "spm",
    "package-a",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
